apiVersion: v1
kind: Service
metadata:
  name: {{ include "drinksaver-frontend.fullname" . }}
  labels:
    {{- include "drinksaver-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drinksaver-frontend.selectorLabels" . | nindent 4 }}
